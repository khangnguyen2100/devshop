const discountType = {
  PERCENT: 'PERCENT',
  FIXED_AMOUNT: 'FIXED_AMOUNT',
};
const discountAppliesToType = {
  ALL: 'ALL',
  SPECIFIC: 'SPECIFIC',
};

export { discountType, discountAppliesToType };
